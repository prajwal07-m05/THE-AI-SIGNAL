# ---- Core async stack ----
httpx[http2]==0.27.2          # async HTTP client with HTTP/2
aiolimiter==1.1.0             # async token-bucket rate limiter
tenacity==9.0.0               # retries w/ exponential backoff + jitter
anyio==4.6.2                  # structured concurrency primitives

# ---- Parsing ----
selectolax==0.3.27            # blazing-fast HTML parsing (lexbor)
python-dateutil==2.9.0        # robust date parsing
lxml==5.3.0                   # fallback parser / XML (arXiv Atom feed)
feedparser==6.0.11            # Atom/RSS feeds (arXiv, news)

# ---- Anti-bot / JS rendering (Phase V) ----
playwright==1.48.0            # async headless browser
playwright-stealth==1.0.6     # fingerprint evasion

# ---- LLM providers (Phase III fallback chain) ----
google-generativeai==0.8.3    # Gemini Flash
groq==0.11.0                  # Groq (Llama 3.x)
openai==1.54.4                # DeepSeek is OpenAI-compatible
tiktoken==0.8.0               # token counting for chunking / 413 avoidance

# ---- Entity resolution (Phase IV) ----
rapidfuzz==3.10.1             # fast fuzzy string matching
unidecode==1.3.8              # unicode -> ascii normalization

# ---- Storage / dedupe ----
redis==5.2.0                  # distributed dedupe + freshness ledger (optional)
pydantic==2.9.2               # schema validation
pydantic-settings==2.6.1      # env-driven settings

# ---- Output ----
gspread==6.1.4                # Google Sheets writer
google-auth==2.36.0           # service-account auth
pandas==2.2.3                 # CSV/xlsx fallback export

# ---- Observability ----
structlog==24.4.0             # structured JSON logging
rich==13.9.4                  # pretty console progress

# ---- Dev ----
pytest==8.3.3
pytest-asyncio==0.24.0
